'use strict';

describe('cart/CartCtrl', CartCtrlTestFunction);

function CartCtrlTestFunction()
{
    beforeEach(function()
    {
        
    });
    
    it("should be this", function()
    {
        var objectUnderTest = { ok : true };
        
        expect(objectUnderTest.ok).toBe(true);
    });
}